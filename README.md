# project35
Author-Harshad jadhav
# Reusable Dropdown Menu

A reusable JavaScript dropdown menu component.

## Usage

createMenu(data, selector, options)

### Arguments

1. data
2. selector
3. options

### Example

createMenu(data, menu, {
    itemClass: "category-item",
    imageClass: "category-image"
});
